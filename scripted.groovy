node {
    stage = ('Build') {
        echo "Build is available"
    }
    stage = ('Build') {
        echo "Build is available"
    }
    stage = ('Build') {
        echo "Build is available"
    }
    stage = ('Build') {
        echo "Build is available"
    }
}