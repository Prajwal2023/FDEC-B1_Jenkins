node {
    stage = ('Pull') {
        echo "pull is available"
    }
    stage = ('Build') {
        echo "Build is available"
    }
    stage = ('Test') {
        echo "Build is available"
    }
    stage = ('Deploy') {
        echo "Build is available"
    }
}