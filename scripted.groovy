node {
    stage = ('Pull') {
        echo "Build is available"
    }
    stage = ('Build') {
        echo "Build is available"
    }
    stage = ('Test') {
        echo "Build is available"
    }
    stage = ('Build') {
        echo "Build is available"
    }
}